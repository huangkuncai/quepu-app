// Compatibility entry point for adapters that import the persistence port by
// its conventional repository filename.
export {
  REPOSITORY_CONTRACT_VERSION,
  REPOSITORY_METHODS,
  RepositoryError,
  assertRepository,
  isRepositoryError
} from './contracts.js';
