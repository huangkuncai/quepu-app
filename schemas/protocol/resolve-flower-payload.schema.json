{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://susong.local/schemas/protocol/resolve-flower-payload.schema.json",
  "title": "Resolve Susong flower payload",
  "type": "object",
  "additionalProperties": false,
  "required": ["action"],
  "properties": {
    "action": { "type": "string", "enum": ["discard", "replace"] }
  }
}
